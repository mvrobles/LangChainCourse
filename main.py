# Agent
# Input: question about an item
# Output: return price and discount

# Two tools: 

# The ReAct Loop (Agent loop) 
# Synerging reasoning and acting in language models
# Thought: decide what tool to use (LLM). Using system message. 
# Action -> Observation -> Thought -> Action 
# Once the model thinks it doesn't need any more tools, it answers 

from dotenv import load_dotenv

load_dotenv()

def main():
    pass

if __name__ == "__main__":
    main()
